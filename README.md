# csharp-project

# Requirments

Before running the application, install:
- Visual Studio with .NET desktop development / WPF support
- SQL Server LocalDB
- SQL Server Management Studio (SSMS) recommended

The application uses this LocalDB SQL Server instance:

```text
(localdb)\MSSQLLocalDB
```
