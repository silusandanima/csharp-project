<UserControl x:Class="CraftConnectPOS.Controls.MetricCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="96">
    <UserControl.RenderTransform>
        <ScaleTransform x:Name="CardScale" ScaleX="1" ScaleY="1"/>
    </UserControl.RenderTransform>
    <UserControl.RenderTransformOrigin>0.5,0.5</UserControl.RenderTransformOrigin>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Mouse.MouseEnter">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="CardScale"
                                     Storyboard.TargetProperty="ScaleX"
                                     To="1.03" Duration="0:0:0.15"/>
                    <DoubleAnimation Storyboard.TargetName="CardScale"
                                     Storyboard.TargetProperty="ScaleY"
                                     To="1.03" Duration="0:0:0.15"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
        <EventTrigger RoutedEvent="Mouse.MouseLeave">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="CardScale"
                                     Storyboard.TargetProperty="ScaleX"
                                     To="1" Duration="0:0:0.15"/>
                    <DoubleAnimation Storyboard.TargetName="CardScale"
                                     Storyboard.TargetProperty="ScaleY"
                                     To="1" Duration="0:0:0.15"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Border Style="{StaticResource GlassCard}" Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            
            <Border Grid.Row="0" CornerRadius="12,12,0,0">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#4CAF50"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Title}" Value="Orders">
                                <Setter Property="Background" Value="#1976D2"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Title}" Value="Revenue">
                                <Setter Property="Background" Value="#E65100"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Title}" Value="Total Revenue">
                                <Setter Property="Background" Value="#E65100"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Title}" Value="Monthly Avg">
                                <Setter Property="Background" Value="#1976D2"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Title}" Value="Low Stock">
                                <Setter Property="Background" Value="#C62828"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Title}" Value="Top Category">
                                <Setter Property="Background" Value="#7B1FA2"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Title}" Value="Orders Complete">
                                <Setter Property="Background" Value="#4CAF50"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>

           
            <Grid Grid.Row="1" Margin="16,10,16,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Title}" Foreground="{StaticResource MutedTextBrush}"
                               FontSize="12" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Value}" Foreground="{StaticResource DeepGreenBrush}"
                               FontFamily="{StaticResource SerifFont}"
                               FontSize="28" FontWeight="Bold" Margin="0,3,0,3"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        
                        <TextBlock FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value=""/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Title}" Value="Products">
                                            <Setter Property="Text" Value="▲"/>
                                            <Setter Property="Foreground" Value="#2E7D32"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Title}" Value="Orders">
                                            <Setter Property="Text" Value="▲"/>
                                            <Setter Property="Foreground" Value="#2E7D32"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Title}" Value="Revenue">
                                            <Setter Property="Text" Value="▲"/>
                                            <Setter Property="Foreground" Value="#2E7D32"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Title}" Value="Total Revenue">
                                            <Setter Property="Text" Value="▲"/>
                                            <Setter Property="Foreground" Value="#2E7D32"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Title}" Value="Monthly Avg">
                                            <Setter Property="Text" Value="▲"/>
                                            <Setter Property="Foreground" Value="#2E7D32"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Title}" Value="Low Stock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="{Binding Note}" Foreground="{StaticResource MutedTextBrush}"
                                   FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                
                <Border Grid.Column="1" Width="44" Height="44" CornerRadius="10"
                        VerticalAlignment="Center" Margin="10,0,0,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Title}" Value="Products">
                                    <Setter Property="Background" Value="#E8F5E9"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Title}" Value="Orders">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Title}" Value="Revenue">
                                    <Setter Property="Background" Value="#FFF3E0"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Title}" Value="Total Revenue">
                                    <Setter Property="Background" Value="#FFF3E0"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Title}" Value="Monthly Avg">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Title}" Value="Low Stock">
                                    <Setter Property="Background" Value="#FFEBEE"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Title}" Value="Top Category">
                                    <Setter Property="Background" Value="#F3E5F5"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Title}" Value="Orders Complete">
                                    <Setter Property="Background" Value="#E8F5E9"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Path Width="22" Height="22" Stretch="Uniform"
                          HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Style.Resources>
                                    <Geometry x:Key="ProductsIcon">M21,16.5C21,16.88 20.79,17.21 20.47,17.37L12.57,21.37C12.37,21.47 12.16,21.5 11.97,21.5C11.78,21.5 11.57,21.47 11.37,21.37L3.47,17.37C3.15,17.21 2.94,16.88 2.94,16.5V7.5C2.94,7.12 3.15,6.79 3.47,6.63L11.37,2.63C11.75,2.44 12.19,2.44 12.57,2.63L20.47,6.63C20.79,6.79 21,7.12 21,7.5V16.5M12,4.15L5.04,7.5L12,10.85L18.96,7.5L12,4.15M4.94,15.55L11,18.58V12L4.94,9V15.55M19.06,15.55V9L13,12V18.58L19.06,15.55Z</Geometry>
                                    <Geometry x:Key="OrdersIcon">M19,7H16V6A4,4 0 0,0 12,2A4,4 0 0,0 8,6V7H5A2,2 0 0,0 3,9V20A2,2 0 0,0 5,22H19A2,2 0 0,0 21,20V9A2,2 0 0,0 19,7M10,6A2,2 0 0,1 12,4A2,2 0 0,1 14,6V7H10V6M19,20H5V9H19V20M12,12A2,2 0 0,1 10,10H8A4,4 0 0,0 12,14A4,4 0 0,0 16,10H14A2,2 0 0,1 12,12Z</Geometry>
                                    <Geometry x:Key="RevenueIcon">M21 18H3c-1.1 0-2-.9-2-2V8c0-1.1.9-2 2-2h18c1.1 0 2 .9 2 2v8c0 1.1-.9 2-2 2zM6 13h6v2H6v-2z M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 13h-2v-2h2v2zm0-4h-2V7h2v4z</Geometry>
                                    <Geometry x:Key="LowStockIcon">M12,2L1,21H23L12,2M12,6L19.53,19H4.47L12,6M11,10V14H13V10H11M11,16V18H13V16H11Z</Geometry>
                                </Style.Resources>
                                <Setter Property="Fill" Value="Transparent"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Title}" Value="Products">
                                        <Setter Property="Fill" Value="#1B5E20"/>
                                        <Setter Property="Data" Value="{StaticResource ProductsIcon}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Title}" Value="Orders">
                                        <Setter Property="Fill" Value="#0D47A1"/>
                                        <Setter Property="Data" Value="{StaticResource OrdersIcon}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Title}" Value="Revenue">
                                        <Setter Property="Fill" Value="#E65100"/>
                                        <Setter Property="Data" Value="{StaticResource RevenueIcon}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Title}" Value="Total Revenue">
                                        <Setter Property="Fill" Value="#E65100"/>
                                        <Setter Property="Data" Value="{StaticResource RevenueIcon}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Title}" Value="Monthly Avg">
                                        <Setter Property="Fill" Value="#0D47A1"/>
                                        <Setter Property="Data" Value="{StaticResource RevenueIcon}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Title}" Value="Orders Complete">
                                        <Setter Property="Fill" Value="#1B5E20"/>
                                        <Setter Property="Data" Value="{StaticResource OrdersIcon}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Title}" Value="Top Category">
                                        <Setter Property="Fill" Value="#7B1FA2"/>
                                        <Setter Property="Data" Value="{StaticResource ProductsIcon}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Title}" Value="Low Stock">
                                        <Setter Property="Fill" Value="#B71C1C"/>
                                        <Setter Property="Data" Value="{StaticResource LowStockIcon}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>

