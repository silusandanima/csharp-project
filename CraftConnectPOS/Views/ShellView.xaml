<UserControl x:Class="CraftConnectPOS.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Ellipse Width="360" Height="360" Fill="#DCECE3" Opacity="0.45" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-205,-130,0,0"/>
        <Ellipse Width="420" Height="420" Stroke="#DCD9BB" StrokeThickness="45" Opacity="0.33" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-210,-260"/>

        <Grid Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" CornerRadius="22" Padding="24">
                <DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Top">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,28">
                            <Image Source="pack://application:,,,/Assets/Brand/CraftConnectLogo.png"
                                   Width="40" Height="40" Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                            <StackPanel Margin="10,0,0,0">
                                <TextBlock Text="Craft" Foreground="{StaticResource DeepGreenBrush}" FontFamily="{StaticResource SerifFont}"
                                           FontSize="18" FontWeight="Bold"/>
                                <TextBlock Text="Connect" Foreground="{StaticResource DeepGreenBrush}" FontFamily="{StaticResource SerifFont}"
                                           FontSize="18" FontWeight="Bold" Margin="0,-4,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Text="MODULES" Foreground="{StaticResource MutedTextBrush}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Button Content="Dashboard" Command="{Binding ShowDashboardCommand}" Style="{StaticResource NavButton}" Margin="0,0,0,6"/>
                        <Button Content="Products" Command="{Binding ShowProductsCommand}" Style="{StaticResource NavButton}" Margin="0,0,0,6"/>
                        <Button Content="Inventory" Command="{Binding ShowInventoryCommand}" Style="{StaticResource NavButton}" Margin="0,0,0,6"/>
                        <Button Content="Suppliers" Command="{Binding ShowSuppliersCommand}" Style="{StaticResource NavButton}" Margin="0,0,0,6"/>
                        <Button Content="Orders" Command="{Binding ShowOrdersCommand}" Style="{StaticResource NavButton}" Margin="0,0,0,6"/>
                        <Button Content="Statistics" Command="{Binding ShowStatisticsCommand}" Style="{StaticResource NavButton}"/>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Bottom">
                        <Border Background="#DDF0E5" CornerRadius="10" Padding="14" Margin="0,0,0,14">
                            <StackPanel>
                                <TextBlock Text="Local database" Foreground="{StaticResource WarningBrush}" FontWeight="Bold" FontSize="12"/>
                                <TextBlock Text="Products, materials, suppliers, and orders are saved automatically."
                                           TextWrapping="Wrap" Foreground="{StaticResource DeepGreenBrush}" FontSize="11" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#E9F7EE" CornerRadius="10" Padding="12">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="34" Height="34" Background="{StaticResource DeepGreenBrush}" CornerRadius="17">
                                    <TextBlock Text="{Binding UserInitial}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Margin="10,0,0,0">
                                    <TextBlock Text="{Binding Username}" Foreground="{StaticResource DeepGreenBrush}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Role}" Foreground="{StaticResource MutedTextBrush}" FontSize="11"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <Button Content="Log out" Command="{Binding LogoutCommand}" Style="{StaticResource SecondaryButton}"
                                Margin="0,12,0,0" Padding="14,9"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Column="1" Margin="28,8,0,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="82"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <StackPanel VerticalAlignment="Top">
                        <TextBlock Text="{Binding PageTitle}" Style="{StaticResource PageTitle}"/>
                        <TextBlock Text="{Binding PageSubtitle}" Style="{StaticResource MutedText}" Margin="0,3,0,0"/>
                    </StackPanel>
                </Grid>

                <ContentControl Grid.Row="1" Content="{Binding CurrentPage}"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
