<UserControl x:Class="CraftConnectPOS.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Ellipse Width="360" Height="360" Fill="#BBDCCB" Opacity="0.42" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-155,-190,0,0"/>
        <Ellipse Width="330" Height="330" Stroke="#DDE7DA" StrokeThickness="2" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,70,190,0"/>
        <Ellipse Width="520" Height="520" Stroke="#D9D7B8" StrokeThickness="58" Opacity="0.48" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-240,-270"/>

        <Border Width="460" Style="{StaticResource GlassCard}" Padding="48" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <Image Source="pack://application:,,,/Assets/Brand/CraftConnectLogo.png"
                       Width="78" Height="78" Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       HorizontalAlignment="Center"/>

                <TextBlock Text="Welcome back" FontFamily="{StaticResource SerifFont}" Foreground="{StaticResource DeepGreenBrush}"
                           FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,12,0,0"/>
                <TextBlock Text="Sign in to manage your craft business" Style="{StaticResource MutedText}"
                           HorizontalAlignment="Center" Margin="0,4,0,24"/>

                <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="{StaticResource PrimaryGreenBrush}" CornerRadius="20" Padding="0,10">
                        <TextBlock Text="Login" Foreground="White" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                    </Border>
                    <Button Grid.Column="1" Content="Sign up" Command="{Binding ShowSignupCommand}" Style="{StaticResource SecondaryButton}" Margin="-8,0,0,0"/>
                </Grid>

                <TextBlock Text="Username" FontWeight="SemiBold" Foreground="#33464D" FontSize="12"/>
                <TextBox Text="admin" Style="{StaticResource InputBox}" Margin="0,5,0,14"/>
                <TextBlock Text="Password" FontWeight="SemiBold" Foreground="#33464D" FontSize="12"/>
                <PasswordBox Password="demo123" Style="{StaticResource InputPassword}" Margin="0,5,0,16"/>

                <Grid Margin="0,0,0,18">
                    <CheckBox Content="Remember me" IsChecked="True" Foreground="#33464D" VerticalAlignment="Center"/>
                    <TextBlock Text="Forgot password?" Foreground="{StaticResource PrimaryGreenBrush}" FontWeight="SemiBold"
                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>

                <Button Content="Sign in" Command="{Binding SignInCommand}" Style="{StaticResource PrimaryButton}" Padding="16,13"/>
                <Button Content="New user? Create account" Command="{Binding ShowSignupCommand}" Style="{StaticResource SecondaryButton}"
                        Margin="0,12,0,0" Padding="16,13"/>

                <TextBlock Text="WPF | MVVM | C# | .NET Framework" Foreground="{StaticResource MutedTextBrush}"
                           FontSize="10" HorizontalAlignment="Center" Margin="0,18,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
